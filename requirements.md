# requirements.md

## 1. 概要

入力画像（PNG/JPG）をグリッド分割し、各セルの明度に応じて「ひらがな1文字」を選択して描画する、ひらがな版ASCIIアート画像フィルターを p5.js で実装する。  
文字色はセル内の平均色（RGB）に基づく。描画は「元画像にオーバーレイ」または「文字のみ」の2モードを切り替え可能とする。  
インタラクティブ調整のために Tweakpane によるUIを提供し、UI変更時のみ再描画（`redraw()`）する。

また、将来的な公開先として **GitHub Pages** を前提に、静的ホスティングで動作する構成とする。

---

## 2. ゴール（達成したいこと）

- 画像を読み込み、グリッド状にひらがな文字で表現された結果を表示できる
- グリッド列数（`cols`）を固定値として指定できる
  - 行数（`rows`）は、隙間が出ないように自動補正（`ceil`）される
- 各セルで以下を満たす
  - 文字はセル中心に配置される
  - 文字サイズはセルに収まる範囲で自動調整される（セルサイズ連動）
  - 文字色はそのセルの代表色（平均色相当）に基づく
- PNGの透明ピクセル（alpha）を含む場合、透明領域に対応するセルは文字を描画しない
- ひらがな文字セット（`charset`）は以下を両対応する
  - **自動密度ソート（デフォルト）**：指定した文字集合を密度計測して暗→明に並べ替えて使用
  - **手動プリセット**：ユーザーが指定した順序をそのまま使用
- 明暗対応の反転（`invert`）により「暗いほど疎 / 明るいほど密」も可能にする
- 描画モード
  - **overlay**：元画像 + 文字（元画像の不透明度を調整可能）
  - **textOnly**：背景色 + 文字（背景色を指定可能）
- フォントは Google Fonts（earlyaccess含む）を利用できる
  - フォント適用は「Load Font」ボタンで明示的に実行（自動切替は行わない）
- **入力画像は2系統**
  - **サンプル画像**：同梱した複数枚から選択して適用できる
  - **ローカル画像**：ユーザーが手元のPNG/JPGを選んで適用できる（ブラウザ内処理）
- Tweakpaneで主要パラメータを操作でき、操作に応じて再描画される
- パフォーマンス上、常時ループは行わず、UI変更・画像変更・フォント適用などのタイミングでのみ再描画する
- **GitHub Pagesで公開可能**
  - 追加のサーバサイド処理を要さず、静的ファイルのみで動作する

---

## 3. 非ゴール（今回やらないこと）

- 動画入力（Webcam含む）のリアルタイム処理
- 生成結果の高品質な“厳密平均色”計算（セル内ピクセルを全走査する厳密平均はしない）
- 文字の縦横比補正（`cellAspect`）の導入（**常に 1.0 固定**）
- 画像の自動回転・EXIF方向補正などの高度な入出力周り
- 複数行文字列や1セル複数文字などの拡張（1セル=1文字のみ）
- 画像アップロードのサーバ保存（ローカル選択はブラウザ内で処理し、アップロードはしない）

---

## 4. ユースケース

- サンプル画像を切り替えながら、ひらがなフィルターの見た目をUIで調整して最適な表現を探る
- 自分のローカル画像を読み込んで同じフィルターを適用し、出力見た目を確認する
- 文字だけの表現（textOnly）で抽象化された画像を作る
- ひらがなセットを差し替え、密度ソートによりフォント依存の崩れを抑えながら表現を変える
- GitHub Pagesで公開し、URLから誰でも同様の操作ができる状態にする

---

## 5. 入力仕様

### 5.1 画像入力
- 画像形式：PNG / JPG
- 解像度・アスペクト比：任意
- 画像供給元：
  - サンプル画像（同梱）
  - ローカル画像（ファイル選択）
- PNGでalphaを含む場合：
  - alphaがしきい値未満のセルは文字を描画しない（しきい値はUIで調整可能）

### 5.2 ひらがな文字集合（charset）
- 文字集合はユーザーが文字列として与える
- 例：
  - 全角スペースあり：`"はぬふせやのひと　"`
  - 全角スペースなし：`"はぬふせやのひとへ"`
- 自動密度ソートでは、この文字集合を密度計測して並び替えた結果を使用する
- 手動プリセットモードでは、指定順序をそのまま使用する

---

## 6. 出力仕様（描画結果）

### 6.1 グリッド
- 指定値：`cols`（列数）
- 自動算出：
  - セル幅 `cellW` は表示画像幅と `cols` から決まる
  - セル高さ `cellH` は `cellW` と同じ（`cellAspect=1.0`固定）
  - `rows = ceil(displayH / cellH)` とし、縦方向の隙間が出ないようにする
- 最終行などで画像領域からはみ出す可能性があるが、**描画は画像領域でクリップ**して隙間を見せない

### 6.2 セル代表色・明度
- 各セルの代表色は「縮小画像（cols×rows）」の対応ピクセルから取得する（平均色相当）
- 明度は代表色から算出し、文字選択に使用する
- 明度→文字インデックスのマッピングに `gamma` を適用できる

### 6.3 文字描画
- 1セルにつき 1文字
- 文字位置：セル中央
- 文字サイズ：セルに収まるよう自動決定（セルサイズに連動）
- 文字色：セル代表色（RGB）
- 追加表現：
  - `letterSpacing`：セル配置間隔を倍率で調整（例：1.0で標準）
  - `jitter`：文字位置に微小ランダムを加える（0で無効）

### 6.4 透明領域
- alpha判定により透明セルは「文字を描画しない」

### 6.5 描画モード
- overlay：
  - 元画像を描画し、その上に文字を描画
  - 元画像の不透明度（`imageAlpha`）を調整可能
- textOnly：
  - 背景色（`backgroundColor`）を塗りつぶし、その上に文字のみ描画

---

## 7. UI要件（Tweakpane）

### 7.1 主要パラメータ（最低限）
- `cols`：列数
- `renderMode`：`overlay` / `textOnly`
- `charset`：ひらがな文字列（ユーザー編集可能）
- `charsetMode`：`autoDensitySort`（デフォルト） / `manual`
- `invert`：明暗対応の反転
- `gamma`：明度補正（表現調整用）
- `alphaThreshold`：透明判定しきい値（0〜255想定）
- `imageAlpha`：overlay時の元画像不透明度
- `textAlpha`：文字不透明度
- `backgroundColor`：textOnly時の背景色

### 7.2 追加パラメータ（表現調整）
- `letterSpacing`：セル間隔倍率
- `jitter`：位置ゆらぎ量
- （任意）密度計測の設定：
  - `densitySampleSize`
  - `densityThreshold`

### 7.3 画像入力UI
- サンプル画像選択（ドロップダウン等）
- ローカル画像選択（`<input type="file" accept="image/*">`）
  - 選択した画像が即座に適用される（または「Load Image」ボタンでも可）

### 7.4 フォント
- フォント候補（初期）
  - Nico Moji（earlyaccess）
  - M PLUS 1p
  - さわらび明朝
- UIに「フォント選択」と「Load Font」ボタンを用意し、ボタン押下で読み込み・適用を行う
- フォント適用後は再描画する

---

## 8. パフォーマンス要件

- 常時 `draw()` ループは行わない（`noLoop()`）
- UI変更、画像変更、フォント適用などのイベントでのみ `redraw()` する
- グリッドサンプリングは縮小画像（cols×rows）から取得するため、画像の元解像度に影響されにくい

---

## 9. 公開・配布要件（GitHub Pages）

- GitHub Pages にデプロイしてURLから閲覧・実行できること
- 静的ファイルのみで動作すること（サーバサイド不要）
- パスは相対パス前提（GitHub Pagesのサブパス配信に耐える）
- ローカル画像はブラウザ内でのみ扱い、外部へアップロードしない

---

## 10. 互換性・動作環境

- ブラウザ上で動作（ローカル静的ホスティング / GitHub Pages を想定）
- 依存：
  - p5.js
  - Tweakpane
  - Google Fonts（必要なフォントのCSS/読み込み）

---

## 11. 受け入れ条件（Acceptance Criteria）

- サンプル画像を選択すると、グリッド状にひらがな文字で表現された結果が表示される
- ローカル画像を選択すると、同様にフィルター結果が表示される（画像は外部送信されない）
- `cols` を変更すると、縦方向に隙間が出ずにグリッドが再構成される
- overlay / textOnly の切替が機能する
- 透明を含むPNGで、透明領域に文字が描画されない
- `charsetMode=autoDensitySort` で、フォント変更後も破綻しにくい順序で文字が割り当てられる
- `charsetMode=manual` で、指定順序がそのまま反映される
- `invert` で明暗対応が逆転する
- Tweakpane操作時のみ更新され、常時描画でCPUが回り続けない
- フォントは「Load Font」ボタンで適用され、適用後に再描画される
- GitHub Pages上で同様に動作する（静的配信でOK）
